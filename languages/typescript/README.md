# TypeScript

Solutions and notes for TypeScript.

## Install, build, and run inside the container

From the project root:

```bash
docker compose exec dev bash -lc "cd languages/typescript && npm install && npm run build && npm run start"
```
