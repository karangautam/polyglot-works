# Maximum Depth Binary Tree

## Problem

Use depth-first search recursively.
The depth of a node is `1 + max(depth(left), depth(right))`.
The base case is an empty node, which has depth `0`.

## Input

Describe the input format.

## Output

Describe the expected output.

## Examples

- `example input` -> `example output`

## Approach

Explain the idea used to solve it.

## Time complexity

- `O(n)`

## Space complexity

- `O(h)` where `h` is the tree height

## Implemented in

- [ ] Python
- [ ] Go
- [ ] Java
- [ ] Rust
- [ ] C++
- [ ] TypeScript

## Notes

Any tricky parts, edge cases, or observations.
