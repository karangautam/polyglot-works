# K Closest Points To Origin

## Problem

Compute the distance of each point from the origin using `x*x + y*y`.
Then use a heap of size `k` or sort all points by distance.
A heap is more scalable when `k` is much smaller than `n`.

## Input

Describe the input format.

## Output

Describe the expected output.

## Examples

- `example input` -> `example output`

## Approach

Explain the idea used to solve it.

## Time complexity

- `O(n log k)` with a heap, or `O(n log n)` with sorting

## Space complexity

- `O(k)` with a heap

## Implemented in

- [ ] Python
- [ ] Go
- [ ] Java
- [ ] Rust
- [ ] C++
- [ ] TypeScript

## Notes

Any tricky parts, edge cases, or observations.
