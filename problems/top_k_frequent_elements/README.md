# Top K Frequent Elements

## Problem

First count how many times each number appears using a hash map.
Then use either a heap or bucket sort to get the `k` most frequent elements efficiently.
Bucket sort is especially neat because frequencies are bounded by `n`.

## Input

Describe the input format.

## Output

Describe the expected output.

## Examples

- `example input` -> `example output`

## Approach

Explain the idea used to solve it.

## Time complexity

- `O(n)` with bucket sort, or `O(n log k)` with a heap

## Space complexity

- `O(n)`

## Implemented in

- [ ] Python
- [ ] Go
- [ ] Java
- [ ] Rust
- [ ] C++
- [ ] TypeScript

## Notes

Any tricky parts, edge cases, or observations.
